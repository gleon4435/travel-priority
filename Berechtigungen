rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /lists/{userId} {
      allow read: if true; // jeder kann lesen
      allow write: if request.auth != null && request.auth.uid == userId; // nur der angemeldete Besitzer schreiben
    }
  }
}
